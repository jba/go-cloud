# Incorrect output.
$ echo hello world
not hello world

# More incorrect output.
$ echo now
$ echo is
$ echo the time
now
isn't
the time

# Command should fail, but doesn't.
$ echo <-- FAIL

# Command should not fail, but does.
$ cd foo
